import { PageIntro } from '../../components/PageIntro/PageIntro';

export function SettingsPage() {
  return (
    <PageIntro
      description="Day Party is the default. Display, audio, and theme controls will be added in their dedicated milestones."
      eyebrow="Settings"
      title="Tune the room your way."
    />
  );
}
